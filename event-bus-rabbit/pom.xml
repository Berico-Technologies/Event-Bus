<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>bundle</packaging>
    <parent>
        <groupId>pegasus.cip</groupId>
        <artifactId>event-bus</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>..</relativePath>
    </parent>
    <artifactId>event-bus-rabbit</artifactId>
    <name>Event Bus Rabbit Client</name>
    <description>The Event Bus RabbitMQ implementation</description>

    <dependencies>
        <dependency>
            <groupId>pegasus.cip</groupId>
            <artifactId>event-bus-client</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>2.3.6</version>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>pegasus.eventbus.client.rabbitmq</Bundle-SymbolicName>
                        <Bundle-Activator>pegasus.eventbus.amqp.osgi.Activator</Bundle-Activator>
                        <Require-Bundle>pegasus.eventbus.client</Require-Bundle>
                        <DynamicImport-Package>*</DynamicImport-Package>
                        <Import-Package>!com.ibm.uvm.tools.*</Import-Package>
                        <Embed-Dependency>
                            !event-bus-client*,
                            !mockito-core*,
                            !slf4j-api*,
                            !logback-core*,
                            !logback-classic,
                            !log4j*,
                            !org.osgi*,
                            !org.apache.felix*,
                            !awaitility*,
                            !junit*,
                            !spring-context*,
                            !aopalliance*,
                            !spring-aop*,
                            !spring-bean*,
                            !spring-asm*,
                            !spring-core*,
                            !spring-expression*,
                            !objenesis*,
                            !hamcrest-core*,
                            !hamcrest-library*,
                            !cglib-nodep*,
                            !commons-logging*,
                            !commons-codec*,
                            !commons-httpclient*,
                            !commons-cli*
                        </Embed-Dependency>
                        <Embed-Transitive>true</Embed-Transitive>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
